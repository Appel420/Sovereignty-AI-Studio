version: '3.9'

# ─────────────────────────────────────────────────────────────────────────────
# Sovereignty AI Studio — Self-Hosted Docker Stack
# NO external SaaS. NO Ollama. NO third-party AI vendors.
# All AI routes through the sovereign bridge (port 9898).
# ─────────────────────────────────────────────────────────────────────────────

services:
<<<<<< copilot/remove-external-platform-dependencies

  # ── PostgreSQL ────────────────────────────────────────────────────────────
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sovereignty}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me-in-production}
      POSTGRES_DB: ${POSTGRES_DB:-sovereignty_db}
    expose:
      - "5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sovereignty}"]
=======
  # ── Data stores ────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-creativeflow_db}
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
>>>>>> main
      interval: 10s
      timeout: 5s
      retries: 5

<<<<<< copilot/remove-external-platform-dependencies
  # ── Redis ─────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
======
  redis:
    image: redis:7
>>>>>> main
    expose:
      - "6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

<<<<<< copilot/remove-external-platform-dependencies
  # ── Python Backend (FastAPI) ──────────────────────────────────────────────
  backend:
    build: ./backend
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-sovereignty}:${POSTGRES_PASSWORD:-change-me-in-production}@db:5432/${POSTGRES_DB:-sovereignty_db}
      REDIS_URL: redis://redis:6379/0
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
      SOVEREIGN_MODEL_PATH: ${SOVEREIGN_MODEL_PATH:-/models/sovereign.gguf}
      SOVEREIGN_ONNX_PATH: ${SOVEREIGN_ONNX_PATH:-/models/sovereign.onnx}
      SOVEREIGN_API_URL: http://node-bridge:9898/api/ai
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:9898}
      LOG_DIR: /app/logs
    volumes:
      - ./logs:/app/logs
      # Mount model directory if SOVEREIGN_MODELS_DIR is set (optional)
      # Set SOVEREIGN_MODELS_DIR=/path/to/your/models in .env to enable local inference
======
  # ── Backend FastAPI ────────────────────────────────────────────────────────
  backend:
    build: ./backend
    expose:
      - "8000"
    environment:
<<<<< copilot/integrate-production-features
      - DATABASE_URL=postgresql://postgres:password@db:5432/creativeflow_db
<<<<< copilot/organize-sov-ai-studio-repo
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=${JWT_SECRET:-sovereignty-one-secret}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - LOCAL_MODEL=${LOCAL_MODEL:-llama3}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
=====
      - REDIS_URL=redis://redis:6379/0
      - SECRET_KEY=${SECRET_KEY:-sovereign-secret-change-in-production}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - XAI_API_KEY=${XAI_API_KEY:-}
>>>>>> main
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
<<<<<< copilot/remove-external-platform-dependencies

  # ── Node.js Sovereign Bridge (only external port: 9898) ───────────────────
  node-bridge:
    build: ./node-bridge
    restart: unless-stopped
    ports:
      - "9898:9898"
    environment:
      NODE_BRIDGE_PORT: "9898"
      BACKEND_URL: http://backend:8000
      SOVEREIGN_API_URL: http://backend:8000/api/ai
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
      RATE_LIMIT_RPM: ${RATE_LIMIT_RPM:-30}
=======
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    restart: unless-stopped
======
      - DATABASE_URL=postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-password}@postgres:5432/${POSTGRES_DB:-creativeflow_db}
      - REDIS_URL=redis://redis:6379
    depends_on:
      - postgres
      - redis
      >> main
>>>> main

  # ── Node gateway / bridge (external entry point on port 9898) ─────────────
  node-bridge:
    build: ./node-bridge
    ports:
      - "9898:9898"
    environment:
      - BACKEND_URL=http://backend:8000
      - WEATHER_URL=http://backend:8001
      - GATEWAY_URL=http://gateway:9000
      - NODE_BRIDGE_PORT=9898
      - CORS_ORIGIN=http://localhost:9898
>>>>>> main
    depends_on:
<<<<< copilot/integrate-production-features
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:9898/health', r => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

<<<<<< copilot/remove-external-platform-dependencies
  # ── Nginx Reverse Proxy ───────────────────────────────────────────────────
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ${TLS_CERT:-/dev/null}:/etc/nginx/ssl/cert.pem:ro
      - ${TLS_KEY:-/dev/null}:/etc/nginx/ssl/key.pem:ro
      - ./frontend:/usr/share/nginx/html:ro
    depends_on:
      - node-bridge

volumes:
  db_data:
    driver: local
=======
  db:
<<<<<< copilot/organize-sov-ai-studio-repo
    image: postgres:16
======
    image: postgres:16-alpine
======
      - backend
      - gateway

  # ── Multi-agent system ────────────────────────────────────────────────────
  judge_agent:
    build:
      context: .
      dockerfile: agents/judge_agent/Dockerfile
    expose:
      - "9001"
      - "8000"
>>>>> main
>>>>> main
    environment:
      - JUDGE_METRICS_PORT=8000
    depends_on:
      - redis

  ai_router_agent:
    build:
      context: .
      dockerfile: agents/ai_router_agent/Dockerfile
    expose:
<<<<< copilot/integrate-production-features
      - "5432"
    volumes:
      - db_data:/var/lib/postgresql/data
<<<<<< copilot/organize-sov-ai-studio-repo
      - ./backend/db/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
======
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d creativeflow_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
>>>>>> main
    # Internal only - accessible via backend services
    expose:
      - "6379"
    volumes:
      - redis_data:/data
<<<<<< copilot/organize-sov-ai-studio-repo

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - node-bridge
    profiles:
      - production

volumes:
  db_data:
  redis_data:
======
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

volumes:
  db_data:
  redis_data:
======
      - "8001"
    environment:
      - REDIS_URL=redis://redis:6379
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - XAI_API_KEY=${XAI_API_KEY:-}
    depends_on:
      - judge_agent
      - redis

  plugin_agent:
    build:
      context: .
      dockerfile: agents/plugin_agent/Dockerfile
    expose:
      - "8002"
    environment:
      - REDIS_URL=redis://redis:6379
      - PLUGIN_SDK_DIR=/app/plugin_sdk
    depends_on:
      - judge_agent
      - redis

  platform_agent:
    build:
      context: .
      dockerfile: agents/platform_agent/Dockerfile
    expose:
      - "8003"
    environment:
      - REDIS_URL=redis://redis:6379
      - TWITCH_CHANNEL=${TWITCH_CHANNEL:-}
      - TWITCH_CLIENT_ID=${TWITCH_CLIENT_ID:-}
      - TWITCH_CLIENT_SECRET=${TWITCH_CLIENT_SECRET:-}
      - YOUTUBE_API_KEY=${YOUTUBE_API_KEY:-}
      - YOUTUBE_STREAM_ID=${YOUTUBE_STREAM_ID:-}
      - KICK_CHANNEL=${KICK_CHANNEL:-}
      - X_BEARER_TOKEN=${X_BEARER_TOKEN:-}
    depends_on:
      - judge_agent
      - redis

  voice_assist_agent:
    build:
      context: .
      dockerfile: agents/voice_assist_agent/Dockerfile
    expose:
      - "8004"
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - judge_agent
      - redis

  # ── Python multi-agent gateway (internal port 9000) ───────────────────────
  # External traffic still enters through node-bridge on port 9898.
  gateway:
    build:
      context: .
      dockerfile: gateway/Dockerfile
    expose:
      - "9000"
    environment:
      - GATEWAY_PORT=9000
      - REDIS_URL=redis://redis:6379
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - XAI_API_KEY=${XAI_API_KEY:-}
      - PLUGIN_SDK_DIR=/app/plugin_sdk
    depends_on:
      - judge_agent
      - redis

volumes:
  postgres_data:
>>>>> main
>>>>> main
>>>>>> main
