/.home/runner/work/Sovereignty-AI-Studio/Sovereignty-AI-Studio/environment.yml
name: sovereignty-ai
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - ffmpeg
  - tqdm
  - colorlog
  - pydantic
  - numpy
  - tokenizers
  - pyarrow
  - pip:
      - poetry
      - pre-commit
      - black - isort
      - pytest-cov

      /. bash conda env create -f environment.yml
conda activate sovereignty-ai
pip install -e .
