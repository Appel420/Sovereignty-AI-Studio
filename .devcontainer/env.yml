name: sovereignty-ai
dependencies:
  - python=3.12
  - pip
  - ffmpeg
  - tqdm
  - colorlog
  - pydantic
  - numpy
  - tokenizers
  - pyarrow
  - pip:
      - poetry
      - pre-commit
      - black
      - isort
      - pytest-cov

pip install -r requirements.txt
pip install -e .