tqdm
colorlog
pydantic
numpy
tokenizers
pyarrow
flask
poetry
pre-commit
black
isort
pytest-cov

pip install -r requirements.txt
# Optional (ML modules): pip install -r requirements-ml.txt
pip install -e .
