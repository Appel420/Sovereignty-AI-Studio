name: CD — Sovereign Release

# CD is fail-closed. A release artifact is deployable only after the
# executable I-008 gate and independent PQC SCAR attestation pass.
on:
  push:
    branches: [Collaboration]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: sovereign-cd-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify-and-attest:
    name: Verify + PQC SCAR
    runs-on: [self-hosted, Linux, ARM64]
    timeout-minutes: 45
    steps:
      - name: Checkout exact commit
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -Eeuo pipefail
          : "${GH_TOKEN:?GITHUB_TOKEN is required}"
          rm -rf .git
          git init .
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "${GH_TOKEN}" | base64 | tr -d '\n')"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "${GITHUB_SHA}"
          git checkout --detach FETCH_HEAD
          git config --unset-all http.extraheader 2>/dev/null || true

      - name: Runtime identity gate
        shell: bash
        run: |
          set -Eeuo pipefail
          test "${GITHUB_ACTIONS}" = true
          test "${RUNNER_OS}" = Linux
          test "${RUNNER_ARCH}" = ARM64
          test "$(uname -m)" = aarch64
          python --version

      - name: Install release dependencies
        shell: bash
        run: |
          set -Eeuo pipefail
          python -m pip install --upgrade pip
          python -m pip install --disable-pip-version-check -r requirements.txt
          python -m pip install --disable-pip-version-check -e '.[test,lint,pqc]'
          python -m pip check

      - name: I-008 executable gate
        shell: bash
        run: |
          set -Eeuo pipefail
          python scripts/verify_i008_contract.py
          python -m pytest -q backend/coordination/test_i008_transparency.py tests/test_i008_transparency.py

      - name: Full Python collection gate
        shell: bash
        run: python -m pytest --collect-only -q tests

      - name: Full test suite
        shell: bash
        run: python -m pytest -q tests

      - name: Build release artifact
        shell: bash
        run: |
          set -Eeuo pipefail
          rm -rf dist
          python -m build
          test -n "$(find dist -maxdepth 1 -type f -print -quit)"

      - name: Independent PQC SCAR attestation
        shell: bash
        env:
          SCAR_PQC_KEY_DIR: /var/lib/sovereignty/scar/pqc
          SCAR_PQC_OUTPUT: /var/lib/sovereignty/scar/attestations/${{ github.sha }}.json
        run: |
          set -Eeuo pipefail
          python scripts/scar_pqc_attest.py --status PASS --commit "${GITHUB_SHA}"
          test -s "${SCAR_PQC_OUTPUT}"
          grep -q '"verified": true' "${SCAR_PQC_OUTPUT}"
          grep -q 'ML-DSA-87' "${SCAR_PQC_OUTPUT}"

      - name: Publish release artifact metadata
        shell: bash
        run: |
          set -Eeuo pipefail
          sha256sum dist/*
          printf 'SOVEREIGN_CD_VERIFY=PASS\n'
          printf 'SOVEREIGN_SCAR_PQC=PASS\n'
          printf 'SOVEREIGN_RELEASE_COMMIT=%s\n' "${GITHUB_SHA}"

  deploy:
    name: Sovereign Deployment
    needs: verify-and-attest
    runs-on: [self-hosted, Linux, ARM64]
    environment: production
    timeout-minutes: 30
    steps:
      - name: Checkout exact verified commit
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -Eeuo pipefail
          : "${GH_TOKEN:?GITHUB_TOKEN is required}"
          rm -rf .git
          git init .
          git remote add origin "https://github.com/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "${GH_TOKEN}" | base64 | tr -d '\n')"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "${GITHUB_SHA}"
          git checkout --detach FETCH_HEAD
          git config --unset-all http.extraheader 2>/dev/null || true

      - name: Require explicit deployment adapter
        shell: bash
        run: |
          set -Eeuo pipefail
          test -x ./scripts/deploy-sovereign.sh

      - name: Deploy
        shell: bash
        run: |
          set -Eeuo pipefail
          ./scripts/deploy-sovereign.sh "${GITHUB_SHA}"

      - name: Deployment receipt
        shell: bash
        run: |
          set -Eeuo pipefail
          test -f /var/lib/sovereignty/scar/deployment-receipt.json
          printf 'SOVEREIGN_CD_DEPLOYMENT=PASS\n'
