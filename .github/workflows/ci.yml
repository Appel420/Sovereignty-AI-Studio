name: ci.yml

on:
  push:
    branches: [main, Claude, GPT/Codex, ara-hardened, copilot/main, Collaboration, duckAI, devassist420]
  pull_request:
    branches: [main, Claude, GPT/Codex, ara-hardened, copilot/main, Collaboration, duckAI, devassist420]
  workflow_dispatch:

concurrency:
  group: sovereign-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  PYTHONPATH: .:./backend

jobs:
  repo-conformance:
    name: Repository conformance
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository at exact commit
        env: { GH_TOKEN: "${{ github.token }}" }
        shell: bash
        run: |
          set -Eeuo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 | tr -d '\n')"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "${GITHUB_SHA}"
          git checkout --detach FETCH_HEAD
          git config --unset-all http.extraheader 2>/dev/null || true
      - name: Diamond Core lockfile
        run: |
          set -Eeuo pipefail
          test -f diamond-core/Cargo.toml
          if test -f Cargo.lock; then test -s Cargo.lock; else cargo generate-lockfile --manifest-path diamond-core/Cargo.toml; test -s Cargo.lock; fi
      - name: Diamond Core contract gate
        run: cargo test --manifest-path diamond-core/Cargo.toml --all-targets --locked
      - name: Python baseline
        run: python --version
      - name: Create isolated validation environment
        run: |
          set -Eeuo pipefail
          python -m venv .ci-venv
          .ci-venv/bin/python -m pip install --upgrade pip
          .ci-venv/bin/python -m pip install --no-cache-dir -e '.[test,lint]'
          .ci-venv/bin/python -m pip install --no-cache-dir pip-audit==2.9.0
          .ci-venv/bin/python -m pip check
      - name: Dependency vulnerability gate
        run: .ci-venv/bin/pip-audit --strict
      - name: I-008 executable contract
        run: .ci-venv/bin/python scripts/verify_i008_contract.py
      - name: Compile
        run: .ci-venv/bin/python -m compileall -q backend
      - name: Ruff
        run: .ci-venv/bin/ruff check .
      - name: Mypy
        run: .ci-venv/bin/mypy sovereign/
      - name: Pytest
        run: .ci-venv/bin/pytest -q

  workflow-surface:
    name: Workflow surface audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository at exact commit
        env: { GH_TOKEN: "${{ github.token }}" }
        shell: bash
        run: |
          set -Eeuo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "${GITHUB_SHA}"
          git checkout --detach FETCH_HEAD
      - name: Enumerate workflow surface
        run: |
          set -Eeuo pipefail
          test -d .github/workflows
          count="$(find .github/workflows -maxdepth 1 -type f \( -name '*.yml' -o -name '*.yaml' \) | wc -l)"
          test "$count" -gt 0
          echo "WORKFLOW_FILE_COUNT=$count"
          find .github/workflows -maxdepth 1 -type f \( -name '*.yml' -o -name '*.yaml' \) -print | sort

  trackerdb-familyai:
    name: trackerdb_familyai integration
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout trackerdb_familyai
        run: git clone --depth=1 --branch main https://github.com/Appel420/trackerdb_familyai.git trackerdb_familyai
      - name: Node baseline
        working-directory: trackerdb_familyai
        run: node --version && npm --version
      - name: Install trackerdb_familyai dependencies
        working-directory: trackerdb_familyai
        run: npm ci
      - name: trackerdb_familyai tests
        working-directory: trackerdb_familyai
        run: npm test
      - name: trackerdb_familyai build
        working-directory: trackerdb_familyai
        run: npm run build

  coordination-surface:
    name: Coordination import and contract audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository at exact commit
        env: { GH_TOKEN: "${{ github.token }}" }
        shell: bash
        run: |
          set -Eeuo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "${GITHUB_SHA}"
          git checkout --detach FETCH_HEAD
      - name: Install coordination test dependencies
        run: python -m pip install --upgrade pip && python -m pip install --no-cache-dir -e '.[test]' && python -m pip check
      - name: Coordination import
        run: python -c 'from backend.coordination import BranchRegistry; print("coordination import: PASS")'
      - name: Coordination tests
        run: python -m pytest -q backend/coordination

  recovery-observer:
    name: Recovery observer
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RECOVERY_RUN_ID=${GITHUB_RUN_ID}"
          echo "RECOVERY_SHA=${GITHUB_SHA}"
          echo "RECOVERY_WORKFLOW=${{ github.workflow }}"

  scar-pqc-native:
    name: SCAR — native ML-DSA-87 attestation
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact commit
        env: { GH_TOKEN: "${{ github.token }}" }
        run: |
          set -Eeuo pipefail
          git init .
          git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "${GITHUB_SHA}"
          git checkout --detach FETCH_HEAD
      - name: Install native PQC provider
        run: python3 -m pip install --upgrade pip && python3 -m pip install --no-cache-dir blake3==1.0.9 liboqs-python
      - name: Verify native ML-DSA-87
        run: |
          python3 - <<'PY'
          import oqs
          assert 'ML-DSA-87' in oqs.get_enabled_sig_mechanisms()
          print('NATIVE_ML_DSA_87=PASS')
          PY
      - name: Run SCAR PQC attestation
        env:
          SCAR_PQC_KEY_DIR: "${{ runner.temp }}/scar/pqc"
          SCAR_PQC_OUTPUT: "${{ runner.temp }}/scar/attestation.json"
        run: |
          mkdir -p "$SCAR_PQC_KEY_DIR"
          python3 scripts/scar_pqc_attest.py --status PASS --commit "$GITHUB_SHA"
          test -s "$SCAR_PQC_OUTPUT"
          grep -q '"verified": true' "$SCAR_PQC_OUTPUT"
          grep -q 'ML-DSA-87' "$SCAR_PQC_OUTPUT"

  ara-validation:
    name: Ara validation
    runs-on: ['self-hosted', 'linux', 'arm64']
    steps:
      - name: Checkout exact commit
        env: { GH_TOKEN: "${{ github.token }}" }
        run: |
          set -Eeuo pipefail
          git init .; git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "$GITHUB_SHA"
          git checkout --detach FETCH_HEAD
      - name: Validate first-party Python
        run: |
          set -Eeuo pipefail
          while IFS= read -r -d '' f; do python3 -m py_compile "$f"; done < <(git ls-files -z -- '*.py' ':!:external/**')

  quart-validation:
    name: Quart tests
    runs-on: ['self-hosted', 'linux', 'arm64']
    steps:
      - name: Checkout exact commit
        env: { GH_TOKEN: "${{ github.token }}" }
        run: |
          set -Eeuo pipefail
          git init .; git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "$GITHUB_SHA"
          git checkout --detach FETCH_HEAD
      - name: Install Quart dependencies
        run: python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt
      - name: Quart test suite
        run: python3 -m pytest -q tests

  codeql-local-security:
    name: Local security gate
    runs-on: ['self-hosted', 'linux', 'arm64']
    steps:
      - name: Checkout exact commit
        env: { GH_TOKEN: "${{ github.token }}" }
        run: |
          set -Eeuo pipefail
          git init .; git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "$GITHUB_SHA"
          git checkout --detach FETCH_HEAD
      - name: Workflow action policy
        run: |
          if grep -RInE '^[[:space:]]*uses:[[:space:]]*[^./[:space:]]+/[^[:space:]]+' .github/workflows --include='*.yml' --include='*.yaml'; then exit 1; fi
      - name: Python syntax security gate
        run: python3 -m compileall -q backend apps scripts tests

  ios-sovereign-validation:
    name: iOS sovereign validation
    runs-on: macos-latest
    steps:
      - name: Checkout exact commit
        env: { GH_TOKEN: "${{ github.token }}" }
        run: |
          set -Eeuo pipefail
          git init .; git remote add origin "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}.git"
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          git -c http.extraheader="AUTHORIZATION: basic ${auth}" fetch --no-tags --depth=1 origin "$GITHUB_SHA"
          git checkout --detach FETCH_HEAD
      - name: Validate backend source
        run: python3 -m compileall -q backend
      - name: Validate Swift source when present
        shell: bash
        run: |
          if find ios -type f -name '*.swift' -print -quit 2>/dev/null | grep -q .; then while IFS= read -r -d '' f; do swiftc -parse "$f"; done < <(find ios -type f -name '*.swift' -print0); fi
