# Copilot/Codex setup fragment.
# This file MUST NOT fail a CI job merely because a cloud-agent mode is disabled.
# Network policy is selected by the invoking workflow; CI itself remains runnable.

steps:
  - name: Report agent execution policy
    shell: bash
    run: |
      set -Eeuo pipefail
      echo "Agent execution policy: shared"
      echo "GPT/Codex: available"
      echo "Collaboration: available"
      echo "Network mode: workflow-defined"
