# Sovereignty AI Studio — Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real credentials to version control

# ═══════════════════════════════════════════════════════
# DASHBOARD ARCHITECTURE
# ═══════════════════════════════════════════════════════
# ALL EXTERNAL TRAFFIC GOES THROUGH PORT 9898
# Port 9898 is the ONLY external-facing port.
# Backend, DB, and Redis are internal-only (Docker network).

# ═══════════════════════════════════════════════════════
# PORT CONFIGURATION
# ═══════════════════════════════════════════════════════
NODE_BRIDGE_PORT=9898
BACKEND_PORT=8000
WEATHER_PORT=8001
WEB_APP_PORT=8080
FRONTEND_PORT=3000
PORT_UNIFIED=9000
PORT_BRIDGE=9898
PORT_AUTH=8443

# ═══════════════════════════════════════════════════════
# DATABASE CONFIGURATION
# ═══════════════════════════════════════════════════════
DATABASE_URL=postgresql://sovereignty:password@localhost:5432/sovereignty_db
POSTGRES_USER=sovereignty
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=sovereignty_db

# ═══════════════════════════════════════════════════════
# REDIS CONFIGURATION
# ═══════════════════════════════════════════════════════
REDIS_URL=redis://localhost:6379/0

# ═══════════════════════════════════════════════════════
# SECURITY & AUTHENTICATION
# ═══════════════════════════════════════════════════════
JWT_SECRET=your-jwt-secret-here-change-in-production
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
KEY_FILE=.sg_master_key

# ═══════════════════════════════════════════════════════
# SOVEREIGN AI CONFIGURATION (no external SaaS)
# ═══════════════════════════════════════════════════════
# Path to your locally-hosted GGUF model file
SOVEREIGN_MODEL_PATH=/models/sovereign.gguf
# Path to your locally-hosted ONNX model file
SOVEREIGN_ONNX_PATH=/models/sovereign.onnx
# URL of your own sovereign API endpoint (self-hosted)
SOVEREIGN_API_URL=http://localhost:9898/api/ai
# Comma-separated provider order (never falls back to external SaaS)
SOVEREIGN_PROVIDER_ORDER=local_gguf,local_onnx,sovereign_api

# ═══════════════════════════════════════════════════════
# SERVICE URLs (inter-service communication)
# ═══════════════════════════════════════════════════════
# Docker internal network
BACKEND_URL=http://backend:8000
WEATHER_URL=http://backend:8001

# Local development overrides (uncomment if needed)
# BACKEND_URL=http://localhost:8000
# WEATHER_URL=http://localhost:8001

# ═══════════════════════════════════════════════════════
# PIPER TTS CONFIGURATION (self-hosted voice)
# ═══════════════════════════════════════════════════════
PIPER_BIN=./piper
PIPER_MODEL=./models/en_US-lessac-medium.onnx
PIPER_DIR=./piper-tts

# ═══════════════════════════════════════════════════════
# KEYCLOAK AUTHENTICATION (self-hosted)
# ═══════════════════════════════════════════════════════
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=sovereignty
KEYCLOAK_CLIENT_ID=sovereignty-studio
KEYCLOAK_CLIENT_SECRET=change-me-in-production

# ═══════════════════════════════════════════════════════
# CORS CONFIGURATION
# ═══════════════════════════════════════════════════════
CORS_ORIGIN=http://localhost:9898

# ═══════════════════════════════════════════════════════
# TLS/SSL CONFIGURATION (optional, for production)
# ═══════════════════════════════════════════════════════
# Provide absolute paths to PEM-encoded cert & key to enable HTTPS.
# When set, nginx (port 443) terminates TLS and node-bridge/unified
# server can also serve HTTPS directly.
# Generate self-signed certs for local dev:
#   openssl req -x509 -newkey rsa:2048 -nodes -days 365 \
#     -keyout ./certs/key.pem -out ./certs/cert.pem \
#     -subj "/CN=localhost"
TLS_CERT=
TLS_KEY=

# ═══════════════════════════════════════════════════════
# LOGGING & MONITORING
# ═══════════════════════════════════════════════════════
LOG_DIR=./logs
VERBOSE=0
DEBUG=false
ENVIRONMENT=production

# ═══════════════════════════════════════════════════════
# OPTIONAL FEATURE FLAGS
# ═══════════════════════════════════════════════════════
ALLOW_SHELL=0
SAVE_TRANSFERS=1
RELOAD=0

# ═══════════════════════════════════════════════════════
# PORT ALLOCATION SUMMARY
# ═══════════════════════════════════════════════════════
# EXTERNAL ACCESS (users/clients):
#   9898  — Sovereign Gateway / Node Bridge (ONLY external port)
#
# INTERNAL SERVICES (Docker network only):
#   3000  — Frontend (React)
#   5432  — PostgreSQL
#   6379  — Redis
#   8000  — Backend FastAPI
#   8001  — Weather Dashboard
#   8080  — Web App Server
#   8443  — Auth Proxy
#   9000  — Unified Server
#
# KEY PRINCIPLE: Everything connects through 9898. No external SaaS.

# ═══════════════════════════════════════════════════════
# MULTI-AGENT SYSTEM CONFIGURATION
# ═══════════════════════════════════════════════════════
# Judge Agent
JUDGE_AGENT_PORT=9001
JUDGE_METRICS_PORT=8000

# AI Router Agent
AI_ROUTER_PORT=8001

# Plugin Agent
PLUGIN_AGENT_PORT=8002

# Platform Agent
PLATFORM_AGENT_PORT=8003

# Voice Assist Agent
VOICE_ASSIST_PORT=8004

# Gateway
GATEWAY_PORT=9898

# KODER Python Bridge (bridge.py WebSocket server)
KODER_URL=ws://localhost:9897
SG_PORT=9897
SG_HOST=localhost

# Prometheus metrics
METRICS_PORT=8000

# Platform connector credentials (streaming platforms)
TWITCH_CLIENT_ID=your-twitch-client-id
TWITCH_CLIENT_SECRET=your-twitch-client-secret
YOUTUBE_API_KEY=your-youtube-api-key
KICK_CLIENT_ID=your-kick-client-id
X_BEARER_TOKEN=your-x-bearer-token
