# Sovereignty One - Core Dependencies
# All packages are pure Python or have offline wheels available

cryptography>=42.0.0
blake3>=0.4.0
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0
biopython>=1.83
scikit-learn>=1.4.0
pysam>=0.22.0
pybedtools>=0.9.0
pyBigWig>=0.3.22

# Optional (for full single-cell support)
# archr (R package - run via rpy2 if needed)
# snapatac2 (pip install snapatac2)

# Post-quantum (optional but recommended)
# oqs-python  (install from https://github.com/open-quantum-safe/liboqs-python)

# For deployment only
flask>=3.0.0
gunicorn>=21.2.0