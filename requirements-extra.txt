# ─────────────────────────────────────────────────────────────────────────────
# Sovereignty AI Studio — Extended / Experimental Package Ecosystem
#
# Quarantined 2026-09-10. This file is NOT installed by default.
# Install selectively and only with an explicit, pinned version:
#   pip install <package>==<version>
#
# Policy: no unpinned entries. No experimental or unvetted packages.
# The only package retained here is the MCP bridge, which must also be
# pinned in requirements.txt for production installs.
# ─────────────────────────────────────────────────────────────────────────────

# MCP bridge — sole retained entry. Pin in requirements.txt for prod.
grok-cli-mcp
