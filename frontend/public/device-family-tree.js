/* Device-local registry dashboard. Fetches only same-origin local registry data and falls back safely. */
(function (global) {
  'use strict';
  var fallback = { version:'1.0.0', storage_root:'device-local', network:'disabled', external_memory:'disabled', providers:[
    {id:'local.github_copilot',name:'GitHub Copilot',role:'implementation_review',storage:'GitHub Copilot Local Storage',network:false},
    {id:'local.claude',name:'Anthropic Claude Fable',role:'long_context_analysis',storage:'Anthropic Claude Fable Local Storage',network:false},
    {id:'local.openai_codex',name:'OpenAI ChatGPT Codex',role:'reasoning_and_coding',storage:'OpenAI ChatGPT Codex Local Storage',network:false},
    {id:'local.grok',name:'X.AI Grok / Ara',role:'adversarial_review',storage:'X.AI Grok Local Storage',network:false},
    {id:'local.duckai',name:'DuckAI',role:'privacy_research',storage:'DuckAI Local Storage',network:false},
    {id:'local.devassist420',name:'DevAssist420',role:'hybrid_coordination',storage:'DevAssist420 Local Hybrid Collaboration',network:false},
    {id:'local.sovereignty_ai',name:'Sovereignty AI',role:'owner_visible_router_and_council',storage:'Sovereignty AI',network:false}
  ], wake_word:{primary:'hey ara',recognition:'local_engine_required',remote_recognition:false,status:'not_configured'} };
  var council = ['Claude','Ara/Grok','ChatGPT/Codex','DevAssist420','DuckAI','GitHub Copilot','Sovereignty AI'];
  function escapeHtml(value) { return String(value == null ? '' : value).replace(/[&<>"']/g, function (c) { return ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c]; }); }
  function card(provider) { return '<article class="provider-card"><h3>'+escapeHtml(provider.name)+'</h3><div class="field"><strong>IDENTITY</strong><span>'+escapeHtml(provider.id)+'</span></div><div class="field"><strong>ROLE</strong><span>'+escapeHtml(provider.role)+'</span></div><div class="field"><strong>LOCAL STORAGE</strong><span>'+escapeHtml(provider.storage)+'</span></div><div class="field"><strong>MEMORY</strong><span>DEVICE ONLY</span></div><div class="field"><strong>NETWORK</strong><span>DISABLED</span></div><div class="field authorization"><strong>AUTHORIZATION</strong><span>NOT EVALUATED</span></div><div class="field"><strong>SCAR HISTORY</strong><span>LOCAL-ONLY / UNAVAILABLE</span></div></article>'; }
  function render(registry, statusText) { var root = document.getElementById('family-tree'); var status = document.getElementById('family-tree-status'); if (!root) return; if (status) status.textContent = statusText; var providers = Array.isArray(registry.providers) ? registry.providers : []; root.innerHTML = '<div class="tree-root"><strong>/Admin/On Device Memory Storage/</strong><div class="tree-children">'+providers.map(card).join('')+'</div><div class="council"><strong>Router/Council/</strong><ul class="council-list">'+council.map(function (name) { return '<li>'+escapeHtml(name)+'</li>'; }).join('')+'</ul></div><p class="disclaimer">Folder names are local storage identities only. They do not prove that a provider or local runtime is installed or running. Wake word: '+escapeHtml(registry.wake_word && registry.wake_word.primary || 'hey ara')+'; recognition: local_engine_required; remote recognition: false.</p></div>'; }
  function load() { render(fallback, 'Registry: local fallback'); if (!global.fetch || global.location.protocol === 'file:') return; global.fetch('./provider-registry.json', {cache:'no-store'}).then(function (response) { if (!response.ok) throw new Error('registry unavailable'); return response.json(); }).then(function (registry) { if (registry.network !== 'disabled' || registry.wake_word.remote_recognition !== false) throw new Error('registry boundary rejected'); render(registry, 'Registry: same-origin local file'); }).catch(function () {}); }
  global.SovereignDeviceFamilyTree = { fallback:fallback, render:render, load:load }; document.addEventListener('DOMContentLoaded', load);
}(typeof window !== 'undefined' ? window : globalThis));
