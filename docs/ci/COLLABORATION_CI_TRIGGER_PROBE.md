# Collaboration CI trigger probe

This marker exists only to produce a non-workflow push on `Collaboration` after the canonical `collaboration-gate.yml` workflow was installed.

Acceptance evidence is the resulting GitHub Actions job execution for the canonical workflow.
